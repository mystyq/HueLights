export interface IState {
    on: boolean,
    bri: number,
    hue: number,
    sat: number,
    effect: string,
    xy: number[],
    ct: number,
    alert: string,
    colormode: string,
    mode: string
    reachable: boolean
}